# Studio Plus Six Series

Mobile product-film portfolio with all original image and video assets.

## Open locally

Open `index.html` in a browser. For local video playback and mobile preview, run:

```powershell
node mobile-preview-server.js
```

Then visit the local URL printed by the server.

All media is stored under `assets/` and uses relative paths from `index.html`.
